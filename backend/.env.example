# Backend environment example
# Copy to your deployment environment configuration system. Do not commit real secrets.

# JWT config
JWT_SECRET_KEY=replace-with-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS configuration
# Either set a single origin:
FRONTEND_ORIGIN=http://localhost:3000
# Or set a comma-separated list:
# BACKEND_CORS_ORIGINS=http://localhost:3000,https://your-frontend.example.com

# Optional app name
APP_NAME=TATA ELXSI MOCK API
