# Copy this file to .env and fill in values.
# Do NOT commit .env with real secrets.

# JWT configuration (required for signup/login and protected routes)
JWT_SECRET_KEY=replace_with_secure_random_string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS configuration
# Prefer BACKEND_CORS_ORIGINS as a comma-separated list, or set FRONTEND_ORIGIN to a single origin.
# Examples:
# BACKEND_CORS_ORIGINS=http://localhost:3000,https://myapp.example.com
# FRONTEND_ORIGIN=http://localhost:3000
BACKEND_CORS_ORIGINS=
FRONTEND_ORIGIN=

# Optional app name override
APP_NAME=TATA ELXSI MOCK API
