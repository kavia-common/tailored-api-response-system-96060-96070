# Backend environment configuration for TATA ELXSI MOCK API
# Copy this file to ".env" and adjust values for your environment.
# NOTE: Do not commit your real ".env" file.

# Security
JWT_SECRET=change_me_local_dev_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS configuration
# Set one or more exact frontend origins. Comma-separated if multiple.
# Examples:
#   Local dev: http://localhost:3000
#   Kavia preview (replace with your actual frontend URL):
#   https://vscode-internal-27635-beta.beta01.cloud.kavia.ai:4000
CORS_ORIGINS=

# Optional fallbacks: if CORS_ORIGINS is empty, the app will try these vars
# to derive an allowed origin list. Set any that apply to your environment.
FRONTEND_ORIGIN=
FRONTEND_URL=
SITE_URL=
PUBLIC_SITE_URL=
REACT_APP_SITE_URL=
VITE_SITE_URL=
